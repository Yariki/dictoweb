import { MatchfieldsDirective } from './matchfields.directive';

describe('MatchfieldsDirective', () => {
  it('should create an instance', () => {
    const directive = new MatchfieldsDirective();
    expect(directive).toBeTruthy();
  });
});
